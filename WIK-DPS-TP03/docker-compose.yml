version: '3.8'

services:
  api:
    build: 
      context: ./api
    deploy:
      replicas: 4

  reverse-proxy:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - api